# {{NAME}}

{{DESC}}

- **类型**:{{TYPE}}
- **建立日期**:{{DATE}}
- **GitHub**:{{GITHUB_URL}}

## 怎么用

{{TYPE_SPECIFIC_USAGE}}

## Claude 协作约定(温和式提醒)

进入这个项目工作时,Claude 按下面约定行事:

- **完成一个小功能 / 3+ 处改动**,主动提示:"建议 git add + commit 一下"
- **对话变长(60+ 轮)** 后,提示:"可以 /compact 清下上下文"
- **用户问'下一步干啥'**,优先检查是否该 commit 当前进度
- **温和原则**:同样提醒**出现过一次但用户没动作,不要催第二次**,等用户主动提

## 日常 3 句循环

改完东西就 3 句话:

```bash
git add <文件名>
git commit -m "做了啥"
git push
```

不知道自己改了啥?随时跑 `git status`(免费、安全、能看清现状)。
